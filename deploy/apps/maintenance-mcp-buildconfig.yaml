apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  labels:
    build: maintenance-mcp
  name: maintenance-mcp
  namespace: wp-dev
spec:
  failedBuildsHistoryLimit: 5
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: maintenance-mcp:latest
  postCommit: {}
  resources: {}
  runPolicy: Serial
  source:
    binary: {}
    type: Binary
  strategy:
    dockerStrategy:
      dockerfilePath: Containerfile
    type: Docker
  successfulBuildsHistoryLimit: 5

